services:
  - type: web
    name: logsystem
    env: node
    region: frankfurt
    plan: starter
    buildCommand: npm install --legacy-peer-deps
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: RENDER
        value: "true"
      # Base de données Aiven MySQL
      - key: DB_HOST
        sync: false
      - key: DB_PORT
        sync: false
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: DB_NAME
        sync: false
      - key: DB_SSL
        value: "true"
      - key: DB_SSL_CA_PATH
        sync: false
      - key: DB_SSL_REJECT_UNAUTHORIZED
        value: "false"
      - key: DB_KEEPALIVE
        value: "true"
      - key: DB_CONNECTION_LIMIT
        value: "5"
      # Sécurité
      - key: SESSION_SECRET
        generateValue: true
      - key: CSRF_SECRET
        generateValue: true
      - key: BCRYPT_ROUNDS
        value: "10"
      # Application
      - key: PORT
        value: "10000"
      - key: UPLOAD_MAX_SIZE
        value: "52428800"
      - key: IMPORT_BATCH_SIZE
        value: "500"
      - key: ALERT_EVAL_INTERVAL
        value: "60000"
      - key: RETENTION_CRON_HOUR
        value: "3"
      # Redis (Render Key Value) — cache du dashboard
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: logsystem-cache
          property: connectionString

  # Render Key Value instance (Redis-compatible) — cache uniquement,
  # pas de données critiques : le plan free suffit (25 Mo, pas de persistance disque).
  - type: keyvalue
    name: logsystem-cache
    region: frankfurt
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []   # connexions internes uniquement, via le réseau privé Render
